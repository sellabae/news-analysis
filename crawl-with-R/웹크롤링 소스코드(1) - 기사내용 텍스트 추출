#기사내용 가져오기 : 본문의 내용을 불러오는 함수를 찾아야한다.
#앞에 id가 적혀있으면 '#'  앞에 class는 '.'을 붙인다
#id 인지 class 인지는 본문 함수 앞에 붙어있다

chb <-html_nodes(ch, ".par") 
chb

#글자를 가져오기
body <- html_text(chb)


#파일 정리하기
body

#현재 상태에서 body를 쳐보면, /가 많이 나오는데 ""같은 인용구호를 이스케이프하는 도구니 무시하자.

#본문을 제목 쓴 파일에 이어쓰기
#append=T : 기존파일에 새로운 결과물을 추가하여 저장할 수 있는 옵션/ 쓰지 않으면 기존 사라지고 새로운 결과물로 대체
write(body, "ch.txt", append=T)
